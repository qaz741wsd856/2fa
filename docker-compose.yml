services:
  2fa:
    build: .
    container_name: 2fa-authenticator
    ports:
      - "3111:3111"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/2fa.db
    restart: unless-stopped
